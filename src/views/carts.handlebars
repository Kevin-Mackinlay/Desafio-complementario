<div>
  {{#each products}}
    <p>{{this.first_name}}</p>
  {{/each}}
  {{#if hasPrevPage}}
    <a href="/carts?page={{prevPage}}"><button>Anterior</button></a>
  {{/if}}

  {{#if hasNextPage}}
    <a href="/carts?page={{nextPage}}"><button>Siguiente</button></a>
  {{/if}}
</div>